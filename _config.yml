# Site Build Settings

# jekyll-paginate configuration
paginate:            10

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

sass:
  sass_dir: assets/css
  style: expanded

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           "Algorave India"
description:	       "A collective of computational artists from India bringing creative coding to the forefront to blur lines between artists and engineers #livecoding"
author:              "Abhinay Khoparzi"

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
